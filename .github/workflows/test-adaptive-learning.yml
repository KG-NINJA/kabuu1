name: Test Adaptive Learning System

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Test AdaptiveNVDALearner
        run: |
          PYTHONPATH=. python << 'EOF'
          import logging
          logging.basicConfig(level=logging.INFO, format="%(asctime)s - %(message)s")
          
          from src.nvda_reinforcement import AdaptiveNVDALearner
          
          print("=== AdaptiveNVDALearner のテスト ===")
          learner = AdaptiveNVDALearner()
          
          # 学習レポート
          report = learner.get_learning_report()
          print("\n📊 学習進捗レポート:")
          for key, value in report.items():
              print(f"  {key}: {value}")
          
          # 改善判定
          should_improve, strategy = learner.should_improve_model()
          print(f"\n🔧 改善が必要: {should_improve}")
          print(f"💡 推奨戦略:")
          import json
          print(json.dumps(strategy, indent=2, ensure_ascii=False))
          EOF

      - name: Run prediction cycle with adaptive learning
        run: |
          echo "=== 適応的学習を有効にした予測実行 ==="
          PYTHONPATH=. python -m src.prediction_pipeline --mode cycle --run-prediction

      - name: Check learning progress
        run: |
          echo "=== 学習進捗の確認 ==="
          
          if [ -f "performance_metrics.json" ]; then
            echo "✓ performance_metrics.json:"
            tail -3 performance_metrics.json
          fi
          
          if [ -f "reinforcement_learning.log" ]; then
            echo ""
            echo "✓ reinforcement_learning.log:"
            tail -5 reinforcement_learning.log
          fi

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: adaptive-learning-test
          path: |
            performance_metrics.json
            reinforcement_learning.log
            nvda_learning/
          if-no-files-found: warn
          retention-days: 7
